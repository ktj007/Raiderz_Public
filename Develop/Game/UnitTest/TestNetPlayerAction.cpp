#include "stdafx.h"
#include "XNetActionNA.h"
#include "XNetActionGrappled.h"
#include "XNetPlayer.h"
#include "XModuleNetControl.h"

SUITE(NetPlayerAction)
{
/*	TEST(TestNetActionFlag)
	{
		std::auto_ptr<XNetActionGrappled> apNetActionGrappled(new XNetActionGrappled(NULL));
		std::auto_ptr<XNetActionNA> apNetActionNA(new XNetActionNA(NULL));

		CHECK_EQUAL(apNetActionGrappled->CheckFlag(NACT_CANNOT_CHANGE_ANIMATION), true);
		CHECK_EQUAL(apNetActionNA->CheckFlag(NACT_CANNOT_CHANGE_ANIMATION), false);
	}
*/
}