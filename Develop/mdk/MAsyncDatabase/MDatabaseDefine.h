#pragma once


namespace mdb
{
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#define CHK_SQLRET_FAIL(ret, ret_val)				if ((SQL_SUCCESS != ret) && (SQL_SUCCESS_WITH_INFO != ret) && (SQL_NO_DATA != ret)) \
													{ \
														return ret_val; \
													}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#define MDB_MAX_MESSAGE_LENGTH SQL_MAX_MESSAGE_LENGTH
}